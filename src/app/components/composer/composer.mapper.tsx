import { ComposerComponents } from '~/schema';

import { MarkupMapping } from '~/components/markup/markup.mapper';

export const composerPropsMapping = {
  [ComposerComponents.markup]: MarkupMapping,
};
